# Task 08 — Script Debug

**难度：★★★★☆**

`scripts/batch-copy.sh` 基本能运行，但遇到带空格的文件名会出错。

## 任务

修复脚本，使它能够：

1. 正确处理普通文件名；
2. 正确处理带空格的文件名；
3. 一次处理多个文件；
4. 不通过重命名测试文件绕过问题。

并在 `answers/08.md` 简短回答：为什么 Shell 中 `$var` 和 `"$var"` 有时结果不同？

可能需要：`$@`、`"$@"`、变量展开、引号、`for`。

```bash
./check.sh 08
```
