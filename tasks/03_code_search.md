# Task 03 — Code Search

**难度：★★☆☆☆**

找出 `workspace/project/` 中所有包含 `TODO` 或 `FIXME` 的普通文件。

要求：每个路径只出现一次，按字典序排列，每行一个路径，写入 `output/03_code_search.txt`。

可能需要：`find`、`grep`、`sort`、`uniq`。

```bash
./check.sh 03
```
